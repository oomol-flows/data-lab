icon: ./icon.png
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install --force && pip install -r requirements.txt
  dependencies:
    tools: 0.0.3
dependencies:
  oomol-file: 0.0.7
name: data-lab
version: 0.0.6
displayName: Data Lab
description: Data analysis package based on open source libraries such as python
  library pandas plotly.
releaseNotes: update example flows
repository: https://github.com/oomol-flows/data-lab
